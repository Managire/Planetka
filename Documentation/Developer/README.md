# Planetka Developer Notes

- `ERROR_HANDLING.md` documents the project standard for typed exceptions and structured error messages.
- `HARDENING_BACKLOG.md` tracks reliability work before and after release.
- `INTERNAL_TEST_ACCOUNTS.md` defines internal test accounts and API-key sources.
- `ACCOUNT_TIER_INTEGRITY.md` now documents the Personal/Commercial licence model and confirms that there are no feature tiers.
- Resolve download-size planning can use `Resources/tile_sizes.sqlite` to avoid slow remote HEAD probes.
- Earth shader runtime tweaks are applied in `asset_builder.py`.
