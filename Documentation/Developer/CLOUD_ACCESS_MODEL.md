# Cloud Access Model

Planetka uses one technical cloud-access model. Users are either `active` or `blocked`.

There are no runtime feature classes in the add-on or backend. Preview, Balanced, Full Quality, clouds, export, panoramic camera support, and animation tools use the same cloud access path.

Licence wording is handled by marketplace and legal documents, not backend feature gates.
